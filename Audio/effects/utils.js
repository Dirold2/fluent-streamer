"use strict";import{BASS_MIN as o,BASS_MAX as M,TREBLE_MIN as e,TREBLE_MAX as h}from"../../Types/audio.js";export function userToGainLinear(t,a=12){const n=Math.max(-1,Math.min(1,t)),r=Math.sign(n)*Math.pow(Math.abs(n),.5)*a;return Math.pow(10,r/20)}export function userToGainDb(t,a=15){const n=Math.max(-1,Math.min(1,t));return Math.sign(n)*Math.pow(Math.abs(n),.5)*a}export function compressSample(t,a=.8,n=4){const r=Math.abs(t);if(r<=a)return t;const s=r-a,i=a+s/n;return Math.sign(t)*i}export function clampVolume(t){return Math.max(0,Math.min(1,t))}export function normalizeBass(t){const a=M-o;if(a===0)return 0;const n=(t-o)/a*2-1;return Math.max(-1,Math.min(1,n))}export function normalizeTreble(t){const a=h-e;if(a===0)return 0;const n=(t-e)/a*2-1;return Math.max(-1,Math.min(1,n))}
//# sourceMappingURL=utils.js.map
